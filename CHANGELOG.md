# 变更记录

## [未发布]

## [1.0.2] - 2026-09-21

### Added

- 在主窗口标题栏显示构建版本，并增加后台更新检查、托盘通知和手动检查入口；发现新版本后可打开 GitHub 发布页面。

### Fixed

- 改进版本检查对 GitHub API 错误响应的处理和提示。

## [1.0.1] - 2026-09-18

- 增加按应用保存的快捷键策略：拦截全部、黑名单和白名单。
- 增加固定组合键捕获与配置界面，支持例如 `Ctrl+Alt+A` 的黑名单/白名单条目。
- 未包含快捷键策略设置的已有规则继续按“拦截全部”处理。
- 修改策略后需要重启目标应用；已经注册的快捷键不会被主动撤销。

## [1.0.0] - 2026-09-18

首个正式版本。

### Added

- 支持按单个 `.exe` 文件或文件夹添加规则；文件夹规则会递归匹配其中的应用程序。
- 支持启用、停用和删除规则，并将规则保存到当前用户的配置文件中。
- 对规则创建后启动的匹配应用生效，并显示规则和拦截状态。
- 支持 Windows x86 和 x64 应用；x64 安装包同时包含处理 32 位应用所需的组件。
- 支持系统托盘运行、重新打开主窗口、退出程序，以及登录时自动启动。
- 将配置和诊断日志保存在本机，日志保留当前文件和两个轮转文件。
- 提供 NSIS 安装包、开始菜单和桌面快捷方式。

### Changed

- Windows 目标默认使用静态 MSVC 运行库，发布程序不要求目标机器另外安装 Visual C++ Redistributable。
- 规则状态文本始终显示，并增加 Windows Explorer 重启后重新创建托盘图标的处理。

### Fixed

- 修复托盘图标初始化失败后的处理。
- 修复卸载后登录时仍尝试启动 Hotkey Blocker 的问题。
- 修复卸载后遗留配置和日志文件的问题。

### Security

- 发布包不使用 Authenticode 签名；安装包提供同名 SHA-256 校验文件，便于核对下载文件的完整性。
- 程序只拦截标准 `RegisterHotKey` 调用，不绕过 Windows 权限边界，也不处理低级键盘钩子或键盘驱动实现的快捷键。

### 已知限制

- 添加规则时，如果目标应用已经运行，需要重启该应用才能应用规则。
- 目标应用以更高权限运行、属于受保护进程、启用反注入/动态代码限制，或受到安全软件限制时，规则可能无法生效。
- 当前发布版本支持 x86/x64 应用，不支持 ARM64 应用。
