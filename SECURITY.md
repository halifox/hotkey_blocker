# 安全说明和威胁模型

## 项目定位

Hotkey Blocker 是一个本地 Windows 工具，不是安全产品、反恶意软件工具或权限提升工具。它的核心行为是监控目标进程，并将随程序分发的 Hook DLL 注入目标进程，以拦截 `RegisterHotKey`。

因此，杀毒软件、EDR、反作弊软件或 Windows 安全策略可能会阻止它。这是预期的安全边界，不应通过关闭系统保护来解决。

## 信任边界

以下内容必须被视为可信：

- Hotkey Blocker 主程序
- `HotkeyHook32.dll`、`HotkeyHook64.dll`
- `HotkeyBlockerInjector32.exe`
- 安装目录和其父目录的 ACL
- 构建工具链和发布流水线

如果攻击者能够修改安装目录中的 DLL 或辅助程序，主程序可能在目标进程中加载攻击者提供的代码。这是 DLL 注入架构的固有风险。请将程序安装在受保护目录，不要从可被普通用户写入的共享目录运行未签名副本。

## 主要威胁

### 恶意或被替换的发布文件

风险：被替换的主程序、Hook DLL 或辅助程序可以获得目标进程中的执行权限。

缓解措施：

- 只从项目 Release 下载程序。
- 校验 Release 提供的 SHA-256 文件。
- 生产发布应使用 Authenticode 签名。
- 不要从临时下载目录、网络共享或普通用户可写目录运行程序。

### 权限边界误解

风险：用户可能认为“注入失败”可以通过提权或关闭安全策略解决。

行为：程序不会绕过 Windows 权限、受保护进程或反注入策略。高权限目标通常需要以相同或更高权限运行本程序，并且仍可能被系统策略拒绝。

### 目标进程稳定性

风险：Hook DLL 在目标进程中执行，Hook 或卸载异常可能影响目标应用。

缓解措施：

- 只向用户明确选择的进程注入。
- 目标应用重启后才会重新加载状态。
- 不在目标进程中执行网络访问或持久化操作。
- 报告问题时提供目标应用、架构、错误码和可复现步骤。

### 敏感诊断信息

风险：日志可能包含本地路径、进程名称和 PID。

缓解措施：公开 Issue 或报告前应删除用户名、公司路径、项目路径和其他敏感信息。

## 安全报告

请不要在公开 Issue 中发布可直接利用的漏洞细节、恶意 DLL 或私人日志。优先使用 GitHub 的 [Security Advisories](https://github.com/halifox/hotkey_blocker/security/advisories/new) 提交私密报告；如果该入口不可用，请先开一个不包含技术细节的 Issue，请维护者提供私密联系方式。

报告中请尽量包含：

- Hotkey Blocker 版本或 Git commit
- Windows 版本和 x86/x64 架构
- 目标进程架构和权限级别
- 最小复现步骤
- 脱敏后的日志和错误码
- 是否使用了自行构建或修改过的 DLL

项目会先确认问题影响范围，再决定修复、发布说明和必要的安全更新。请给维护者合理的修复时间，不要在修复前公开披露可利用细节。
