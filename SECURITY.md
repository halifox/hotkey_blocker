# 安全说明

## 产品边界

Hotkey Blocker 是本地 Windows 工具，不是安全产品、反恶意软件工具或权限提升工具。程序监控选定的目标进程，并将随程序分发的 Hook DLL 加载到目标进程中，以拦截 `RegisterHotKey` 注册请求。

杀毒软件、EDR、反作弊软件或 Windows 安全策略可能阻止相关操作。这属于预期的安全边界，不应通过关闭系统保护来解决。

## 信任边界

以下组件和位置必须被视为可信：

- Hotkey Blocker 主程序
- `HotkeyHook32.dll`、`HotkeyHook64.dll`
- `HotkeyBlockerInjector32.exe`
- 安装目录及其父目录的 ACL
- 构建工具链和发布流水线

如果攻击者能够修改安装目录中的 DLL 或辅助程序，主程序可能在目标进程中加载攻击者提供的代码。这是 DLL 注入架构的固有风险。请将程序安装在受保护目录，不要从普通用户可写的共享目录运行未签名副本。

## 主要威胁与缓解措施

### 发布文件被替换

被替换的主程序、Hook DLL 或辅助程序可以获得目标进程中的执行权限。

请只从项目 Release 下载程序并校验随发布包提供的 SHA-256 文件。正式发布由 Release 工作流使用 Authenticode 签名；未配置签名证书时，工作流会失败，不会将未签名安装包作为正式 Release 发布。不要从临时下载目录、网络共享或普通用户可写目录运行程序。

### 权限边界误解

程序不会绕过 Windows 权限、受保护进程或反注入策略。高权限目标通常需要以相同或更高权限运行本程序，并且仍可能被系统策略拒绝。

### 目标进程稳定性

Hook DLL 在目标进程中执行，Hook 或卸载异常可能影响目标应用。程序只向用户明确选择的进程注入，不在目标进程中执行网络访问或持久化操作。目标应用重启后才会重新加载规则状态。

报告问题时，请提供目标应用、目标架构、权限级别、错误码和最小复现步骤。

### 敏感诊断信息

日志可能包含本地路径、进程名称和 PID。公开 Issue 或报告前，请删除用户名、公司路径、项目路径和其他敏感信息。

## 安全报告

请不要在公开 Issue 中发布可直接利用的漏洞细节、恶意 DLL 或私人日志。优先使用 GitHub 的 [Security Advisories](https://github.com/halifox/hotkey_blocker/security/advisories/new) 提交私密报告；如果该入口不可用，请先提交不包含技术细节的 Issue，由维护者提供私密联系方式。

报告中请尽量包含：

- Hotkey Blocker 版本或 Git commit
- Windows 版本和 x86/x64 架构
- 目标进程架构和权限级别
- 最小复现步骤
- 脱敏后的日志和错误码
- 是否使用自行构建或修改过的 DLL

项目会先确认问题的影响范围，再决定修复方案、发布说明和必要的安全更新。请为维护工作留出合理时间，不要在修复前公开披露可利用细节。
